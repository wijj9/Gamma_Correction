{
  "builds": [
    { "src": "server/app.py", "use": "@vercel/python" },
    { "src": "client/static/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/client/static/$1" },
    { "src": "/(.*)", "dest": "/server/app.py" }
  ]
}
